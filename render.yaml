services:
  - type: web
    name: anydesk-server
    env: node
    plan: free
    rootDir: anydesk-server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: CORS_ORIGINS
        value: "*" # Update this via Render dashboard later to your actual Vercel URL for better security
