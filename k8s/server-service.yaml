apiVersion: v1
kind: Service
metadata:
  name: anydesk-server-service
spec:
  type: NodePort
  selector:
    app: anydesk-server
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30081
